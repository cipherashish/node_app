@Library('lib')_
 
pipeline {
    agent any
    stages{
 
        stage('Checkout'){
            steps{
      
                github_checkout("main", "https://github.com/cipherashish/node_app.git")  
              
                         }
                 }
         
          } 
}
